FROM node:20-alpine
WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy all source code
COPY . .

# Build the project (This runs esbuild per your package.json)
RUN npm run build

EXPOSE 5000

# FIX: Use the compiled JavaScript file in dist/
CMD ["node", "dist/index.js"]